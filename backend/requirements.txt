# Web framework
fastapi==0.115.0
python-multipart==0.0.9
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.9.2

# Rate limiting
slowapi==0.1.9

# LangChain ecosystem — unpinned so pip resolves compatible versions together
langgraph
langchain
langchain-groq
langchain-core
langsmith
groq

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.10

# Redis
redis==5.1.1

# Charts
plotly==5.24.1

# Data processing
pandas==2.2.3
numpy==1.26.4

# Docker SDK (optional — only needed for run_python sandbox tool)
docker==7.1.0

# Retry
tenacity==9.0.0
